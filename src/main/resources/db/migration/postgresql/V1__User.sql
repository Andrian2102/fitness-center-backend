CREATE TABLE Users (
    user_id VARCHAR(50) PRIMARY KEY ,
    name VARCHAR(50) NOT NULL,
    email VARCHAR(100) NOT NULL,
    password VARCHAR(255) NOT NULL,
    no_Handpohne VARCHAR(50) NOT NULL,
    status VARCHAR(255) NOT NULL
);